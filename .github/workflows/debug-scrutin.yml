name: Diagnostic scrutin

on:
  workflow_dispatch:
    inputs:
      scrutin_url:
        description: "URL du scrutin à diagnostiquer"
        required: true
        default: "https://www.assemblee-nationale.fr/dyn/17/scrutins/7987"
      mode:
        description: "Type de diagnostic"
        required: true
        default: "dump"
        type: choice
        options:
          - dump
          - debug

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests beautifulsoup4
      - run: python fetch_activite_deputes.py --${{ github.event.inputs.mode }}-scrutin "${{ github.event.inputs.scrutin_url }}"
